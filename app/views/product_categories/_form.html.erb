<%= form_with(model: product_category, local: true) do |form| %>
  <% if product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_category.errors.count, "error") %> prohibited this product_category from being saved:</h2>

      <ul>
      <% product_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cat_name %>
    <%= form.text_field :cat_name, id: :product_category_cat_name %>
  </div>

  <div class="field">
    <%= form.label :cat_code %>
    <%= form.text_field :cat_code, id: :product_category_cat_code %>
  </div>

  <div class="field">
    <%= form.label :unit_id %>
    <%= form.text_field :unit_id, id: :product_category_unit_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
