<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :order_product_id %>
  </div>

  <div class="field">
    <%= form.label :customer_references %>
    <%= form.text_field :customer_references, id: :order_customer_references %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :order_quantity %>
  </div>

  <div class="field">
    <%= form.label :total_cost %>
    <%= form.text_field :total_cost, id: :order_total_cost %>
  </div>

  <div class="field">
    <%= form.label :payment %>
    <%= form.text_field :payment, id: :order_payment %>
  </div>

  <div class="field">
    <%= form.label :is_delivered %>
    <%= form.check_box :is_delivered, id: :order_is_delivered %>
  </div>

  <div class="field">
    <%= form.label :delivery_date %>
    <%= form.text_field :delivery_date, id: :order_delivery_date %>
  </div>

  <div class="field">
    <%= form.label :order_status %>
    <%= form.text_field :order_status, id: :order_order_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
